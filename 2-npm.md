# NPM

```bash
$ npm init
# creates package.json

$ npm install
# installs packages listed in package.json

$ npm install --save example-package
# installs example-package and adds it to package.json
# --save is default behavior for npm install
```
